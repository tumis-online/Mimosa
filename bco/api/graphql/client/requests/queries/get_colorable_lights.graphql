query get_colorable_lights {
  unitConfigs(filter: {
    properties: {
      unitType: COLORABLE_LIGHT
    }
  }) {
    id
    labelString
    placementConfig {
      location {
        id
        labelString
      }
    }
  }
}