query get_locations {
  unitConfigs(
    filter: {
      properties: {
        unitType: LOCATION
      }
    }
  ) {
    id
    labelString
    placementConfig {
      location {
        id
        labelString
      }
    }
  }
}